# Task V2-031f — News→Ticker Gate + Gold-Set (eval harness & next step)
*SachNetra Adapt Sprint · research/quant lane (adapted template)*

**Depends on**: V2-031d G1 historical backfill (✅ done — `nse_tickers_v2` populated, 6.46% coverage)
**Estimated time**: labelling ~1 day (Lijo); rule/default changes ~1–2h once labelled
**Prep doc**: `ai_docs/learning/research-notes/problem-solving/2026-06-10_news-ticker-tagging-gate-and-goldset.md`
**V1 or V2**: V2 (quant pipeline / corpus hygiene)
**Lane note**: research lane — read-only scripts only; the variant/browser/CSS sections of the template do **not** apply. No `npm run dev`/typecheck-variant checks; validation is the node harnesses below.

---

## Context Manifest
*Read these BEFORE any work. Skip the "Don't load" list to save tokens.*

### Load (in order)
1. `CLAUDE.md` — auto-loaded; note the V2 prod-execution boundary (Lijo runs prod; Claude writes code + read-only checks)
2. `.agents/rules/sachnetra-boundaries.md` — sacred files / scope guard
3. **Prep — required reading:**
   - `ai_docs/learning/research-notes/problem-solving/2026-06-10_news-ticker-tagging-gate-and-goldset.md` — the full design + 6 corrections + conclusion
4. **Code files** — see "Files To Open Before Starting" below

### Don't load (not relevant — skip)
- `ai_docs/prep/*`, `wiki/concepts/*`, `wiki/playbooks/personal_investing.md`
- `src/config/variants/*` — this task does not touch the client SPA at all
- All `tasks/00*_*.md` (archived V1)

### Skill / template lineage
- Generated by: `ai_docs/dev_templates/adapt_sprint_task.md` (adapted for research lane)
- Commit at end: `/git` skill

---

## Context — Current State (2026-06-10)

The news→ticker tagger is `extractCompanies()` in `scripts/_india-market-keywords.mjs` — a **headline-only
dictionary matcher** over `shared/nse-equity-master.json` (built by `scripts/build-equity-master.mjs`). Sole
write at `scripts/seed-india-signals.mjs:341`. The V2-031d backfill populated the `nse_tickers_v2` shadow
column (71,725 rows, 4,637 tagged = **6.46% coverage**).

This session diagnosed that the real problem isn't a weak dictionary — it's that **most headlines aren't about
one company**, so the fix is a **two-stage pipeline**: a **GATE** (SPECIFIC vs EMPTY) *before* the tagger,
judged against a **hand-labelled gold set**. The design + tooling are built and committed; **nothing is
labelled yet** — that's the blocker.

## What This Task Tracks (changes made this session — all committed on `docs/research-system`)

- [x] **`fix(G1)`** (commit `14263394`) — dropped 9 common-word aliases that false-positived on general news
  (Coal→COALINDIA matched "coal mine blast", + Solar, Choice, Excel, Empower, Deep, Platinum, Graphite, Cello),
  via `SUPPLEMENTAL_ALIAS_DROPS` in `build-equity-master.mjs`. Each company stays findable via its multi-word
  form. Verified: FP headlines now tag nothing; real refs still tag. Smoke test 15/15, typecheck 0.
- [x] **`docs(research)`** (commit `71fc8a7a`) — problem-solving note + `gate-preview.mjs` (local gate+tagger
  preview) + `build-goldset-sample.mjs` (read-only stratified sampler) + the 948-row `goldset_sample.csv`.
- [x] **`feat(research)`** (commit `3067f2a6`) — `score-goldset.mjs` (gate P/R/F1 + EMPTY-vs-MISS + tagger P/R)
  + extracted the shared gate into `scripts/research/_gate.mjs` (preview & scorer share one gate, no drift).
- [x] **D1 drafts** — 120 `suggested_gate` labels (all EMPTY) added to the gold CSV for human review.
- [x] **Gate fall-through flip** — `_gate.mjs` default SPECIFIC→EMPTY + new `S6_GROUP_EVENT` rule
  (see "Open Decision — RESOLVED" below). D1 gate fp118→fp3; regression 26/26.
- [x] **`fix(G1)` prod FP audit** (commit `3d93f29c`, 2026-06-10) — read-only audit of `nse_tickers_v2`
  found common-word bare aliases in the TOP tag-volume ranks: MPSLTD 134 ("MPs"), RISHABH 73 (Pant),
  YATRA 71 (pilgrimage), SONAMLTD 65 (Wangchuk), NH 59 (NH-44), LT 51 ("Lt."), SKIPPER 47 (cricket —
  100% FP). Rough estimate: **~16–18% of ALL tagged rows in the shadow column are these FPs.** Fixes:
  12 more `SUPPLEMENTAL_ALIAS_DROPS`; a **dangling-connector cascade guard** (killed 70 over-broad
  prefixes like BANKINDIA "Bank of" → matched Bank of Baroda/Japan, SBIN "State Bank of"); 3 new
  `DENYLIST_CONTEXT` guards (YATRA/TRIDENT/AXISBANK — mixed real/FP, drop would cost recall);
  `removeAliasIgnoreCase` now removes ALL case-variants (was first-only); taxonomy regexes hoisted to
  module init (7.5→4.2 ms/headline). Smoke test 15→34 assertions, all green.
  Audit probe kept at `scratch/_v2031_fp_audit.mjs` (read-only, re-runnable).

## The Open Decision — RESOLVED 2026-06-10 (flip to EMPTY + Group rule)

Running the gate over the real 948-row sample showed the gate works on macro (D2: 67/80 EMPTY) and tagged
strata, but **D1 (random untagged news) was 96% fall-through `SPECIFIC ⚠ LOW`** — because the old
`FALLTHROUGH_DEFAULT` biased SPECIFIC (to avoid dropping group-events like "Adani Group stocks shed ₹1.5 lakh
cr"), and D1 is full of plain non-financial news (FIFA, crime, court) that should be EMPTY.

**Evidence:** Claude drafted all 120 D1 rows (`suggested_gate` column) — every one EMPTY. Provisional scorer
on those drafts: the OLD gate scored **fp118 / tn2** on D1 (118 false positives).

**Decision (Lijo, on the provisional D1 evidence):** flip the fall-through default **SPECIFIC → EMPTY**, and
add a narrow **`S6_GROUP_EVENT`** rule (a named "<word> Group" + a market-impact word) so the rare un-tickered
group event stays SPECIFIC. Implemented in `scripts/research/_gate.mjs`. The fall-through still emits
**LOW confidence** so a loosely-named small-cap story is flagged for review, not silently dropped.

**Result:** D1 gate score **fp118/tn2 → fp3/tn117**; regression still 26/26; sample-wide SPECIFIC 86.3% → 68.6%.
"Adani Group stocks shed…" stays SPECIFIC via S6_GROUP_EVENT; "Working Group meet" correctly stays EMPTY.

> **Still provisional:** the D1 labels are Claude drafts, not human-confirmed. Lijo confirming a D1 sample
> (and labeling A/B/C for real tagger recall/precision) remains open below.

---

## What This Task Does Next (the next step)

**Label D1 first.** D1 is the stratum that directly resolves the fall-through decision and quantifies how much
of the untagged 93.5% is correctly EMPTY vs a real small-cap MISS (the 6.46%-coverage question).

---

## Success Criteria

This task is complete when ALL of the following are true:

- [x] The 9 common-word alias drops are committed and the smoke test passes
- [x] Gate + sampler + scorer exist, are read-only, and run locally
- [x] Fall-through default **decided** with the D1 numbers as evidence → flipped to EMPTY + `S6_GROUP_EVENT`
  rule in `scripts/research/_gate.mjs`; re-verified (gate-preview 26/26, D1 fp118→fp3) — 2026-06-10
- [ ] **D1 drafts confirmed by Lijo** (spot-check `suggested_gate` → copy into `gate_label`; correct the 3
  flagged borderlines) — turns the provisional flip into a confirmed one
- [ ] A 30–50 headline **self-consistency dry-run** on a SPECIFIC-heavy slice (A/B/C), instruction gaps folded
  into the labeling rules
- [ ] **A/B/C strata labelled** for real tagger precision/recall (the COALINDIA-style FP rate + small-cap MISS
  rate — the recall question D1 can't answer)

---

## Second-Order Impact

- **Affected consumers**: `_gate.mjs` is imported by `gate-preview.mjs` and `score-goldset.mjs` only (research
  scripts) — NOT yet wired into the live pipeline. Changing the gate here has zero prod effect until a separate
  James task wires it into `seed-india-signals.mjs`.
- **The alias fix DOES affect prod** once deployed: the regenerated `nse-equity-master.json` changes forward
  tagging. No schema change.
- **Variant bleed risk**: none — no `src/` or variant file touched.
- **New env vars**: none. (Sampler/scorer read `DATABASE_URL` from `.env.local`, same as the backfill.)

---

## Files To Open Before Starting

```
scripts/research/output/goldset/goldset_sample.csv  — the 948-row labelling sheet (fill gate_label/ticker_label/role)
scripts/research/_gate.mjs                           — the heuristic gate cascade (where the fall-through default lives)
scripts/research/score-goldset.mjs                   — reads the labelled CSV → metrics
scripts/research/gate-preview.mjs                    — local preview + 26-example regression
scripts/build-equity-master.mjs                      — SUPPLEMENTAL_ALIAS_DROPS (the common-word fix)
scripts/_india-market-keywords.mjs                   — the tagger (extractCompanies)
```

---

## How To Run (research lane — read-only)

```bash
# label the CSV (start with D1 rows), then:
node scripts/research/score-goldset.mjs --errors     # gate P/R/F1 + EMPTY-vs-MISS + tagger P/R + mislabels
node scripts/research/gate-preview.mjs                # 26/26 regression (run after any _gate.mjs change)
node scripts/research/gate-preview.mjs "headline"     # ad-hoc preview while labelling
node scripts/build-equity-master.mjs                  # rebuild master after any alias change
node scripts/smoke-test-tagger.mjs                    # tagger assertions after a master change
```

---

## Read vs Write

**WRITE only to (research lane):**
- `scripts/research/output/goldset/goldset_sample.csv` — the labels (human)
- `scripts/research/_gate.mjs` — only the fall-through default / Group rule, only after D1 labelled
- `scripts/build-equity-master.mjs` + regenerated `shared/nse-equity-master.json` — only for further alias drops

**Never write to:**
- `src/config/variants/full.ts` / `tech.ts` / `finance.ts` — sacred
- `scripts/seed-india-signals.mjs` — live cron; wiring the gate into it is a SEPARATE James task
- Prod DB — read-only only; Lijo runs any prod-touching step

---

## Deploy reminders (carry forward — do not lose)

1. The regenerated `shared/nse-equity-master.json` (common-word fix + 2026-06-10 FP-audit round: 21 drops
   total, dangling-connector guard, 3 context guards) must go **live** for the forward tagger
   (James/Lijo deploy — same as the V2-031c URBANCO/NAVA drops).
2. The `nse_tickers_v2` shadow column was backfilled with the **old** master — it still contains ALL the
   audited FPs (≈16–18% of its tagged rows: Coal/Solar/MPs/Rishabh/Yatra/Sonam/NH/Lt./Skipper/"Bank of"…).
   The backfill resume-guard (`WHERE nse_tickers_v2 IS NULL`) will **not** correct already-tagged rows —
   the cutover (Decision 8) should be a **full re-tag with the fixed master**, not a copy of v2 as-is.
   ⚠️ Any experiment joining on `nse_tickers_v2` before the re-tag inherits the FP pollution.

---

## Completion Log

- [x] Common-word alias fix committed (`14263394`) — 2026-06-10
- [x] Gate + sampler + scorer committed (`71fc8a7a`, `3067f2a6`) — 2026-06-10
- [x] Gold-set sample generated (948 rows) — 2026-06-10
- [x] D1 drafted (Claude, all EMPTY) + provisional scorer preview — 2026-06-10
- [x] Fall-through default decided + implemented (flip to EMPTY + S6_GROUP_EVENT) — 2026-06-10
- [ ] D1 drafts confirmed by Lijo — [date]
- [ ] A/B/C labelled for real tagger P/R — [date]
- [ ] **TASK V2-031f COMPLETE** ✅
